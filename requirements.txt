# requirements.txt — GPU/dev training machine
#
# Install with:
#   pip install -r requirements.txt
#
# GPU note:
#   PyTorch GPU wheels are installed separately from pytorch.org:
#   https://pytorch.org/get-started/locally/
#
#   CUDA (Linux + NVIDIA):
#     pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
#   Apple Silicon (macOS MPS):
#     pip install torch torchvision   (MPS support is built-in since PyTorch 1.12)
#
# Python: 3.10+ (tested on 3.12)

# Core ML framework (CPU wheels — replace with GPU wheels from pytorch.org for training)
torch>=2.2.0
torchvision>=0.17.0

# ONNX export and quantization
onnx>=1.16.0
onnxruntime>=1.18.0

# Data
numpy==1.26.4
pillow==12.2.0
scikit-learn==1.6.0

# Visualization
matplotlib==3.8.4
opencv-python==4.11.0.86

# Utilities
tqdm==4.67.3
